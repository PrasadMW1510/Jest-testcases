import DeactivateSuccessStudentModalContainer from '../index';

describe('<DeactivateSuccessStudentModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateSuccessStudentModalContainer).toBeDefined();
  });
});
