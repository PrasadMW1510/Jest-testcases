import DeactivateStudentModalContainer from '../index';

describe('<DeactivateStudentModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateStudentModalContainer).toBeDefined();
  });
});
