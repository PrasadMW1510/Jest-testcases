import EditQuizCollectionNamesContainer from '../index';

describe('<EditQuizCollectionNamesContainer />', () => {
  it('the default export should be defined', () => {
    expect(EditQuizCollectionNamesContainer).toBeDefined();
  });
});
