import DeactivateModalContainer from '../index';

describe('<DeactivateModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateModalContainer).toBeDefined();
  });
});
