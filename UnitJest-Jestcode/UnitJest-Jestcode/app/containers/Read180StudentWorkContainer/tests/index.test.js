import Read180StudentWorkContainer from '../index';

describe('<Read180StudentWorkContainer />', () => {
  it('the default export should be defined', () => {
    expect(Read180StudentWorkContainer).toBeDefined();
  });
});
