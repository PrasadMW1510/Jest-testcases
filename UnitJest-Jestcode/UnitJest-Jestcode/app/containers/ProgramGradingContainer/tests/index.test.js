import ProgramGradingContainer from '../index';

describe('<ProgramGradingContainer />', () => {
  it('the default export should be defined', () => {
    expect(ProgramGradingContainer).toBeDefined();
  });
});
