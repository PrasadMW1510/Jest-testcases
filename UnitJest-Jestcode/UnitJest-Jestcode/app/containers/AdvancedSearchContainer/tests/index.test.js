import AdvancedSearchContainer from '../index';

describe('<AdvancedSearchContainer />', () => {
  it('the default export should be defined', () => {
    expect(AdvancedSearchContainer).toBeDefined();
  });
});
