/*
 *
 * ReactivateClassModalContainer reducer
 *
 */

import { fromJS } from 'immutable';

const initialState = fromJS({ error: false });

function accountReactivateClassModalContainerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default accountReactivateClassModalContainerReducer;
