import DeactivateModalSuccessContainer from '../index';

describe('<DeactivateModalSuccessContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateModalSuccessContainer).toBeDefined();
  });
});
