import ProgramAvailableRosterContainer from '../index';

describe('<ProgramAvailableRosterContainer />', () => {
  it('the default export should be defined', () => {
    expect(ProgramAvailableRosterContainer).toBeDefined();
  });
});
