import EditAssignmentContainer from '../index';

describe('<EditAssignmentContainer />', () => {
  it('the default export should be defined', () => {
    expect(EditAssignmentContainer).toBeDefined();
  });
});
