import AboutSamLinkContainer from '../index';

describe('<AboutSamLinkContainer />', () => {
  it('the default export should be defined', () => {
    expect(AboutSamLinkContainer).toBeDefined();
  });
});
