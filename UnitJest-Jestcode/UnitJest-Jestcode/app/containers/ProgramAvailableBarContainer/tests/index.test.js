import ProgramAvailableBarContainer from '../index';

describe('<ProgramAvailableBarContainer />', () => {
  it('the default export should be defined', () => {
    expect(ProgramAvailableBarContainer).toBeDefined();
  });
});
