import StudentWorksContainer from '../index';

describe('<StudentWorksContainer />', () => {
  it('the default export should be defined', () => {
    expect(StudentWorksContainer).toBeDefined();
  });
});
