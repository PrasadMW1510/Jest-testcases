import ProgramCerificateContainer from '../index';

describe('<ProgramCerificateContainer />', () => {
  it('the default export should be defined', () => {
    expect(ProgramCerificateContainer).toBeDefined();
  });
});
