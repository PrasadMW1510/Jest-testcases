import ExportTeacherMadeQuizzesContainer from '../index';

describe('<ExportTeacherMadeQuizzesContainer />', () => {
  it('the default export should be defined', () => {
    expect(ExportTeacherMadeQuizzesContainer).toBeDefined();
  });
});
