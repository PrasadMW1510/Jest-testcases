import StudentGoalContainer from '../index';

describe('<StudentGoalContainer />', () => {
  it('the default export should be defined', () => {
    expect(StudentGoalContainer).toBeDefined();
  });
});
