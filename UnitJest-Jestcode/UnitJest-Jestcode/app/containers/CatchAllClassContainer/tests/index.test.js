import CatchAllClassContainer from '../index';

describe('<CatchAllClassContainer />', () => {
  it('the default export should be defined', () => {
    expect(CatchAllClassContainer).toBeDefined();
  });
});
