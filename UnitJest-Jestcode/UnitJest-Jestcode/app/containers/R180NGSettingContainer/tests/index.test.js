import R180NGSettingContainer from '../index';

describe('<R180NGSettingContainer />', () => {
  it('the default export should be defined', () => {
    expect(R180NGSettingContainer).toBeDefined();
  });
});
