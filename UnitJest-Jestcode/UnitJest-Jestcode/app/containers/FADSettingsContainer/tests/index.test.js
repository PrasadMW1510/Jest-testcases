import FADSettingsContainer from '../index';

describe('<FADSettingsContainer />', () => {
  it('the default export should be defined', () => {
    expect(FADSettingsContainer).toBeDefined();
  });
});
