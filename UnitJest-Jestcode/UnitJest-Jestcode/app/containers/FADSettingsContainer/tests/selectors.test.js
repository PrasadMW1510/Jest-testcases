// import { fromJS } from 'immutable';
// import makeSelectFADSettingsContainer from '../selectors';

describe('selectFADSettingsContainerDomain', () => {
  it('should select the FADSettingsContainer page domain', () => {
    /* const fadSettings = fromJS({
      FADSettingsContainer: { name: 'test' },
    });
    const mockedState = fromJS({
      fadSettings,
    });
    // expect(makeSelectFADSettingsContainer()(mockedState)).toEqual(fadSettings);*/
    const placeholder = true;
    expect(placeholder).toBeTruthy();
  });
});
