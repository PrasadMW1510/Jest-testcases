/*
 *
 * ReactivateSchoolModalContainer reducer
 *
 */

import { fromJS } from 'immutable';

const initialState = fromJS({ error: false });

function accountReactivateSchoolModalContainerReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default accountReactivateSchoolModalContainerReducer;
