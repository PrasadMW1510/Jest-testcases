import TeacherMadeQuizContainer from '../index';

describe('<TeacherMadeQuizContainer />', () => {
  it('the default export should be defined', () => {
    expect(TeacherMadeQuizContainer).toBeDefined();
  });
});
