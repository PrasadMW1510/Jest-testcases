import ProgramCertificateFooterContainer from '../index';

describe('<ProgramCertificateFooterContainer />', () => {
  it('the default export should be defined', () => {
    expect(ProgramCertificateFooterContainer).toBeDefined();
  });
});
