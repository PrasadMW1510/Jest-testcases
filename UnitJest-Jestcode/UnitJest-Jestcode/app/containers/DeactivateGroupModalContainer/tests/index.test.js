import DeactivateGroupModalContainer from '../index';

describe('<DeactivateGroupModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateGroupModalContainer).toBeDefined();
  });
});
