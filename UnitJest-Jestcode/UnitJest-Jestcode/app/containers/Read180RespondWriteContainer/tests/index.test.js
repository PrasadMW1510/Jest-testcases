import Read180RespondWriteContainer from '../index';

describe('<Read180RespondWriteContainer />', () => {
  it('the default export should be defined', () => {
    expect(Read180RespondWriteContainer).toBeDefined();
  });
});
