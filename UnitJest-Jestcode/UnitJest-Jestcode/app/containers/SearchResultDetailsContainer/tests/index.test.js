import SearchResultDetailsContainer from '../index';

describe('<SearchResultDetailsContainer />', () => {
  it('the default export should be defined', () => {
    expect(SearchResultDetailsContainer).toBeDefined();
  });
});
