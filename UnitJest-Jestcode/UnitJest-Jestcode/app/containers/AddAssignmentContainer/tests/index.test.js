import AddAssignmentContainer from '../index';

describe('<AddAssignmentContainer />', () => {
  it('the default export should be defined', () => {
    expect(AddAssignmentContainer).toBeDefined();
  });
});
