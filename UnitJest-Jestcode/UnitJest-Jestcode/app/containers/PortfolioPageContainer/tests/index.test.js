import PortfolioPageContainer from '../index';

describe('<PortfolioPageContainer />', () => {
  it('the default export should be defined', () => {
    expect(PortfolioPageContainer).toBeDefined();
  });
});
