import DeactivateSchoolModalContainer from '../index';

describe('<DeactivateSchoolModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateSchoolModalContainer).toBeDefined();
  });
});
