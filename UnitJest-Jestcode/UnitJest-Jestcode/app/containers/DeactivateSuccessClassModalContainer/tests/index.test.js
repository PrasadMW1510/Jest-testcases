import DeactivateSuccessClassModalContainer from '../index';

describe('<DeactivateSuccessClassModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateSuccessClassModalContainer).toBeDefined();
  });
});
