import R180NGTopicContainer from '../index';

describe('R180NGTopicContainer />', () => {
  it('the default export should be defined', () => {
    expect(R180NGTopicContainer).toBeDefined();
  });
});
