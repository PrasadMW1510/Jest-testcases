import TeacherAccessTableContainer from '../index';

describe('<TeacherAccessTableContainer />', () => {
  it('the default export should be defined', () => {
    expect(TeacherAccessTableContainer).toBeDefined();
  });
});
