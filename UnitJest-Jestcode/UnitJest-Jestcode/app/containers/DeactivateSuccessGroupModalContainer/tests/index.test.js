import DeactivateSuccessGroupModalContainer from '../index';

describe('<DeactivateSuccessGroupModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateSuccessGroupModalContainer).toBeDefined();
  });
});
