import DeactivateClassModalContainer from '../index';

describe('<DeactivateClassModalContainer />', () => {
  it('the default export should be defined', () => {
    expect(DeactivateClassModalContainer).toBeDefined();
  });
});
