import PrintQuizAndAnswerKeyContainer from '../index';

describe('<PrintQuizAndAnswerKeyContainer />', () => {
  it('the default export should be defined', () => {
    expect(PrintQuizAndAnswerKeyContainer).toBeDefined();
  });
});
